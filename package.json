{
  "name": "openclaw-mission-control",
  "version": "1.0.0",
  "private": true,
  "description": "Mission Control Dashboard for OpenClaw — gateway-first integration",
  "scripts": {
    "dev": "concurrently -n server,web -c blue,green \"npm run dev -w server\" \"npm run dev -w web\"",
    "build": "npm run build -w server && npm run build -w web",
    "start": "npm run start -w server",
    "gateway:test": "npm run gateway:test -w server"
  },
  "workspaces": [
    "server",
    "web"
  ],
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.7.3"
  }
}
